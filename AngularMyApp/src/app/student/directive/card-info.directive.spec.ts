import { CardInfoDirective } from './card-info.directive';

describe('CardInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new CardInfoDirective();
    expect(directive).toBeTruthy();
  });
});
